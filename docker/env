export image=fastsense/robot_ros2
export container=rosbot_bridge

export USE_X_SERVER=ON

export USE_ROS1=ON
export USE_ROS2=ON
export USE_GAZEBO=ON

export USE_TORCH=OFF
export USE_CUDA=OFF
export USE_OPENCV=OFF

export USE_REALSENSE=ON
export USE_OAKD=OFF
